import styled from 'styled-components';

export const ManualsPageContainer = styled.div`
    
`;

// Основной контейнер для панели инструментов
export const ToolbarManualsStyled = styled.div`
  /* Ваши стили для ToolbarManuals */
`;

// Компонент ActionManual
export const ActionManualStyled = styled.div`
  /* Ваши стили для ActionManual */
`;

// Список категорий
export const CategoryItems = styled.ul`
  list-style: none;
  padding: 0;
`;

// Элемент категории
export const CategoryItem = styled.li`
  margin-bottom: 20px;
`;

// Заголовок категории
export const CategoryCaption = styled.div`
  display: flex;
  align-items: center;
`;

// Логотип категории
export const CategoryLogo = styled.img`
  width: 40px; /* Пример размера */
  height: 40px; /* Пример размера */
  margin-right: 10px;
`;

// Заголовок категории
export const CategoryTitle = styled.h2`
  font-size: 1.5em;
`;

// Список групп
export const GroupItems = styled.ul`
  list-style: none;
  padding: 0;
`;

// Элемент группы
export const GroupItem = styled.li`
  margin-bottom: 15px;
`;

// Заголовок группы
export const GroupTitle = styled.h3`
  font-size: 1.2em;
`;

// Список мануалов
export const ManualItems = styled.ul`
  list-style: none;
  padding: 0;
`;

// Элемент мануала
export const ManualItem = styled.li`
  display: flex;
  align-items: center;
  margin-bottom: 10px;
`;

// Ссылка на мануал
export const ManualLink = styled.a`
  text-decoration: none;
  color: inherit;
  display: flex;
  align-items: center;
`;

// Иконка мануала
export const ManualIcon = styled.span`
  margin-right: 8px;
`;

// Заголовок мануала
export const ManualTitle = styled.span`
  /* Ваши стили для заголовка мануала */
`;

// Пустое сообщение
export const ManualEmpty = styled.p`
  color: gray;
`;

