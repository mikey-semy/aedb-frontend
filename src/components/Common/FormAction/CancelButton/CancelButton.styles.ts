import styled from 'styled-components';

export const CancelButtonContainer = styled.button`
    border-radius: var(--border-radius-default, 5px);
    box-shadow: var(--box-shadow-default);
`;

export const CancelButtonIcon = styled.span``;

export const CancelButtonTitle = styled.span``;