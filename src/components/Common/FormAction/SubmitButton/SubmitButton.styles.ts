import styled from 'styled-components';

export const SubmitButtonContainer = styled.button`
    border-radius: var(--border-radius-default, 5px);
    box-shadow: var(--box-shadow-default);
`;

export const SubmitButtonIcon = styled.span``;

export const SubmitButtonTitle = styled.span``;